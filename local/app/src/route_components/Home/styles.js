import Immutable from 'immutable';

export default Immutable.Map({
    base: Immutable.Map({
        width: "100%",
        boxSizing: "border-box",
        paddingTop: "73px",
        paddingLeft: "265px"
    }),
    cardWrapper: Immutable.Map({
        textAlign: "center",
    })
});
